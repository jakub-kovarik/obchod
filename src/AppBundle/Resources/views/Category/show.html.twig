{% block title %}

    <h1>Category - show: {{ category.name }}</h1>

{% endblock %}

{% block body %}

<ul>
    <li>ID: <a href={{ path('category_edit', { 'slug': category.slug }) }}>{{ category.id }}</a></li>
    <li>slug: {{ category.slug }}</li>
</ul>

{% endblock %}
